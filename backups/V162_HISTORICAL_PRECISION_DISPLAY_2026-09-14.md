# V162 — Historical fractional-cent presentation preserves exact source values

Business date 14/09/2026. The final broad-range data check found two old historical category-allocation amounts with fractions of a cent. They are not pending user classifications and were not changed, rounded in the ledger, or discarded. Without explicit presentation handling, the strict browser cent adapter would reject a valid older-history report even though recent months work.

Applied native migration expense_browser_preserved_fractional_history_display_20260914. It changes only the browser executive/monthly serialization boundary, leaving internal report functions, source tables and the exact derived expense cache unchanged. Monetary presentation values use two decimal places as the currency display already does. Every affected original decimal string and its payload path are retained in additive source_precision metadata. No balancing row or adjusted economic total is invented. The UI now explicitly displays the provided precision notice where relevant.

Guarded real-source checks passed for all five executive ranges (since 2013, since 2023, last twelve months, last six months and current year), matching displayed month sums, account/card origins and totals. Four monthly checks, including the affected historical month and the three reviewed months, verify displayed category/item/total agreement and executive parity. The exact cache hash before/after is identical. The initial assertion-name ambiguity rolled back before the corrected migration succeeded. The private expense alignment audit stores the exact old values, wrapper definitions, checks and unchanged-source hash; no financial details are published here.

Only the two source-precision notices and the asset-cache version changed in the existing frontend integration. The financial checker remains strict for actual inconsistent cents; this is not a general tolerance or a rewrite of historical allocations. Existing real-page/Flow tests are rerun on this candidate. Publication of this small completion must be confirmed by its actual CI/Pages receipts, not assumed from syntax checks.

Reviewed classifications remain zero pending in both direct and stored queues. The native reconciliation, raw history, current source/date limits, provisional open invoices and independent remaining product gates from preceding checkpoints remain binding.
