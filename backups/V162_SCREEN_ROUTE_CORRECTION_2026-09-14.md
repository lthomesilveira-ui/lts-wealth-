# V162 — Screen route correction during candidate QA

14/09/2026 business date. The first actual-page integration run 34911377122 failed before release because the fixture and integration targeted the older dual-lens function, not the final effective definition in index.html. The file contains later ex135 executive/monthly overrides; the final declaration is active. This corrects the earlier source-path interpretation and must be read with the backend-closeout checkpoint. No wrong candidate was promoted to main.

The complete protected index was recovered from the official current Pages artifact, matching blob a130eafe5f7ee5b7f60a95b5ff988669d0c401d9, and all later declarations were examined. Main's current read model/runtime were likewise hash-checked. Local browser execution is restricted by the environment; no bypass was attempted. Syntax checks ran locally; actual source-page tests run on the existing authorized GitHub CI path with intercepted synthetic financial RPCs.

The corrected integration retains the final executive/monthly UI. It applies the tested selection-safe lifecycle to ex135Load/ex135OpenMonth, matches month totals/counts to executive rows and category totals to the full month, removes the old malformed inline category-button quoting, renders every category choice, distinguishes unknown purchase dates and historic aggregate-only lines, and warns about current open invoices/source date. Partial-month percentages without verified equivalent intervals are not advertised. No change to raw financial data, protected index, Flow recovery runtime or date/competence rules.

The updated test now drives the real effective executive and month routes instead of the inactive old dual state, including actual category clicks, stale monthly/range responses, redacted errors, failed parity, open/unknown dates, responsive width and return to Flow. Publication remains contingent on CI success. All native classification, source-reader and cache fixes in the previous checkpoint remain applied and verified; the UI test failure did not roll back those independent successful backend operations.
